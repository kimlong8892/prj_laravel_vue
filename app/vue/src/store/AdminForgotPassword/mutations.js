export default {
    setEmail(state, email) {
        state.email = email;
    },
    setError(state, error) {
        state.error = error;
    },
    setRecaptcha(state, recaptcha) {
        state.recaptcha = recaptcha;
    }
}